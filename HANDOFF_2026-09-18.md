# BreadEconomySim — handover addendum, 18 September 2026

Supplements `docs/HANDOFF_2026-09-15` (the 15 September handover in the project). Everything below is on top of that state.

## Done this week

1. **Cooperative forms** (`src/cooperatives.jl`, new): `cooperative_form_farms / _bakeries / _theatres` ∈ {:member (the original rule, default, unchanged), :worker, :consumer}; `cooperative_theatres`. Worker: membership on hiring, share out of wages, members served first with the work spread over them (bakery members reserve capacity at the round start), surplus by hours as taxed wage income, hours-based lapse. Consumer: membership on purchase, surplus as untaxed patronage rebate by units, members net the expected rebate off the ask. Both: `retained_surplus_share` locked in an indivisible reserve (pays demurrage under SuMSy — deliberate), asset lock on dissolution, `cooperative_founding = :symmetric` capital calls on members (the shareholder founders' route; default :par_only = old behaviour).
2. **Membership contribution**: `membership_contribution` ∈ {:money, :buffer, :mixed_fixed, :mixed_flexible}, `membership_buffer_pledge`, `buffer_contribution_value`. A pledge moves the demurrage-free exemption, not money. Worth nothing under debt money (documented, tested) — run buffer variants under SuMSy.
3. **Per-subsystem random streams** (`random_streams = true`, 11 streams, `stream(model, :name)`). Old single stream kept under `random_streams = false`; golden values are now two sets (the 15 September set reproduces to the last digit under the single stream).
4. Tests: 329 passing (`julia --project=. -O1 test/runtests.jl`, ~60 s). `scripts/golden.jl` regenerates both golden sets.
5. Reruns under the new streams: `results/all64_coops_rounds.csv` (8 variants), `all64_contribution_rounds.csv` (6), `ladder64_8_*` and `ladder64_9_*` (rungs 8, 9, 9a, 9b). Summaries via `scripts/summarise64.py <rounds.csv> <cols>` (new columns: mem_w, mem_c, locked, pledged, patwage, rebates, memcap, reserved, coophours, unemp_m, unemp_n).

## Consequence of the streams that must be faced

Every figure in the report was produced under the single stream. Rungs 0–7 and all Section 8–10B blocks other than coops/contribution have **not** been rerun. The three-seed numbers will move (see the goldens: bread price 10.34 → 9.18 in the debt reference at round 25). Either rerun everything (`all64.jl` all blocks ≈ 20 min, `ladder.jl` ≈ 10 min) before touching the report, or state in the report which stream produced which figure. Mixing them silently is the one thing not to do.

## Results, 64 persons, 100 rounds, 3 seeds — read as indicative only (n = 3)

Ladder, tickets sold / unemployed / cash Gini, SuMSy: rung 8 (share market) 47 / 18.1 / 0.49 → rung 9 (member co-ops) 92 / 14.2 / 0.40 → 9a (+ co-op theatre, member form) 119 / 12.6 / 0.32 → 9b (worker food chain + consumer theatre) 88 / 12.7 / 0.38. Debt village: 43 → 47 → 52 → 53 tickets. The co-op theatre adds to the recovery under both systems; under SuMSy the member form with a co-op theatre gives the largest recovery, the worker/consumer forms about the same as rung 9 without one.

Coops block (SuMSy): the member form's advantage over the worker/consumer forms in tickets and cash Gini is mostly gone once patronage replaces per-member dividends: member co-ops + co-op theatre 53 tickets / Gini 0.34; worker + consumer theatre 63 / 0.36; worker-only food chain 52 / 0.42; consumer bakeries + theatre 42 / 0.41. The quasi-GI reading of the member form (see the 14 September discussion) is therefore only partly borne out: tickets under the worker + consumer mix are as high or higher, Gini slightly worse. Worker theatre (patronage to a dozen workers) is the worst on Gini (0.59) — the surplus of a labour-only firm goes to few hands. Consumer theatre spreads it. Members-first off vs on: 68 vs 63 tickets, 219 vs 83 members (more churn), Gini 0.35 vs 0.36 — the labour rule is not doing much at this size.

Contribution (SuMSy): buffer-only membership draws the most members (98 vs 83), pledged exemption 840–1,265; Gini unchanged within noise. The mechanism works and conserves the exemption; the distributional effect is below three-seed resolution.

Unemployment of worker-co-op members is 0 in every run against 12–19 non-members — by construction, since members are people who were hired; not a finding.

## Open

- Rerun the full ladder and all64 blocks under the streams (above).
- 30 seeds and intervals before any of the co-op numbers goes in the report (reviewer §3).
- Report Sections 9, 11, 12 and the Dutch translation do not know about any of this.
- `Project.toml` still 0.1.0.

---

# Addendum, 19 September 2026 — full rerun under the per-subsystem streams

Every ladder rung (`ladder64_rounds.csv`, 12 rungs incl. 9a/9b × debt, debt_inherited, sumsy × 3 seeds), the saturated-start ladder (`ladder_sat_*`), and all eight `all64` blocks (`greed`, `measures`, `indexed`, `share_market`, `coops`, `contribution`, `saturation`, `prices`) are now under `random_streams = true`. The old report figures came from the single stream; the 15 September report HTML is saved as `BreadEconomySim_report_2026-09-15.html` (extracted from the web archive) for reference.

## Old report → new streams (3 seeds each side; debt / SuMSy unless stated)

| where | figure | 15 Sept report | 19 Sept streams | reading |
|---|---|---|---|---|
| rung 0 | SuMSy dead at 100 | 8 % | **21 %** (seeds: 54, 57, 40 alive) | the price-ratchet loss is bigger and more seed-dependent than the report says |
| rung 0 | debt dead | 76 % | 75 % | unchanged |
| rung 2 | debt deficit / debt, % GDP | 45.4 / 303 | 45.7 / 304 | unchanged |
| rung 4 | tickets | 68.2 / 126.2 | 65.9 / 124.3 | unchanged |
| rung 5 | debt tickets, deficit, debt | 50.0, 27.2, 213 | 47.8, 27.1, 212 | unchanged |
| rung 7 | debt tickets, gini cash | 46.7, 0.29 | 46.2, 0.30 | unchanged |
| rung 8 | tickets | 46.0 / 47.1 | 43.3 / 47.2 | unchanged |
| rung 8 | SuMSy gini cash / wealth | 0.52 / 0.70 | 0.49 / 0.66 | unchanged |
| rung 9 | tickets | 52.1 / 102.8 | 47.4 / 91.7 | SuMSy recovery a little smaller: "halved" still holds (47 → 92 of the 124 at rung 7) |
| rung 9 | SuMSy gini cash / wealth | 0.37 / 0.52 | 0.40 / 0.57 | same direction, slightly weaker |
| greed | no greed alive | 61 / 64 | 61.7 / 64 | unchanged |
| greed | wealthiest quarter greedy (hoarding 0.5), SuMSy alive | 43 (1 of 3 dead, round 86) | **8.7 (0, 4, 22)** | **"costs a third" becomes "kills the village in two seeds of three"** |
| greed | everyone, hoarding 0 | 59 / 63 | 59.3 / 62 | unchanged |
| greed | everyone, hoarding 0.25 | 61 / 64 | 60.7 / 64 | unchanged |
| greed | everyone, hoarding 0.5 / 0.75, SuMSy | dead by rounds 62 / 87 | dead by rounds 49 / 54 | same outcome, sooner |
| greed | everyone, hoarding 1 | 61 / 64 | 61.3 / 64 | unchanged |
| measures | hoarding 0.5 + rationing, SuMSy | 64 alive, bread 14.1 | 64 alive, bread 9.4 | holds; the price cost is lower |
| measures | hoarding 0.5 + wage ceiling, SuMSy | 21 (2 of 3 dead, by 82) | 20.7 (2 of 3 dead, by 37 and 90) | "saves a third" holds as an average of one surviving seed |
| measures | hoarding 0.5 + two prices, SuMSy | 0 | 0 | unchanged |
| measures | hoarding 0 + two prices | 46 / 21 | 46 / 23 | unchanged |
| measures | land 2 + capacity 5 + rationing | 63 / 64 | 58.3 / 64 | SuMSy unchanged; debt loses a few more |
| prices | hoarding 0, SuMSy alive at ×0.5 / ×1 / ×2 | 21 / 63 / 50 | 42.3 / 62 / 56.7 | the ×0.5 collapse is milder (one seed dead instead of two) |
| prices | hoarding 0.5, SuMSy | 0 / 0 / 0 | 0 / 0 / 0 | unchanged |
| saturation | upper start + greed | dead within 5 rounds | dead by rounds 4–5 | unchanged |
| indexed | hoarding 0, SuMSy money / bread | 22–33k / 18–28 | 28k / 13.9 | spiral holds |

## What this says

1. **The structural results do not move.** Ladder rungs 1–8, the greed sweep at every setting but one, the rationing / two-prices / land-and-capacity measures, saturation, indexed income: within a few percent of the report, same signs, same conclusions. The stream change is a re-draw, and the model is stable to a re-draw wherever the report's claim is a level.
2. **Two claims sit on a seed edge and must be rewritten.** Rung 0 SuMSy ("loses 8 %") and the wealthiest-quarter greed case ("costs a third"). Both are cases where the report quotes a mean of three seeds that straddle a collapse. Under the new draws the mean lands on the other side of the edge. Neither the old nor the new number is the finding; the finding is that these two configurations are bistable at n = 3, and they need 30 seeds and a survival fraction, not a mean.
3. **"Dead by round X" numbers are all soft.** Every collapse round moved by 10–40 rounds. Report them as ranges or drop them.
4. Sections 7, 10 and 10B of the report need those two cells and the collapse rounds corrected before anything else changes; the rest of the prose survives the rerun as written.

## Not yet done

- Report not edited; Dutch not started for the new material.
- The 30-seed rerun of the bistable cells (rung 0 SuMSy; greedy-rich; mixed-greed + wage ceiling) — `all64.jl` takes the seed count as its first argument.

---

# Addendum, 19 September 2026 (evening) — unmet-demand threshold and the greed mechanism

## Code

- `unmet_demand_share` (default 0 = the old one-miss rule). Every site that flagged unmet demand now records the units unserved (`MarketRecord.unmet_units`, market-wide); `demand_unmet(model, seller, good)` in `negotiation.jl` applies the threshold to both the 6 % ask rise and the +1 production target. Wages keep the old rule. Tests: one miss in a hundred does not move the ask at 5 %, ten do; default bit-identical to before. 340 tests pass.
- `scripts/all64.jl <block> <seeds> <systems>`: seeds and systems are now arguments; **the runner is resumable** — each run is appended to `results/all64_<block>_rounds.csv` on completion and finished (variant, system, seed) triples are skipped. Delete the CSV to rerun from scratch. Sessions here recycle without warning; this is why.
- New blocks: `capacity` (mixed greed × capacity 4/5/6, land 2, bread ceiling 2.0, combinations; SuMSy done at 10 seeds) and `threshold` (no greed, greedy rich, mixed greed, mixed + wage ceiling at 5 % and 10 %); ladder rungs `0t`/`0u` (rung 0 at 5 %/10 %).

## Result: what actually starves the greedy SuMSy village

Hypothesis tested (Stef's): the greed starvation is a production-capacity problem. Predictions written before the runs: (1) capacity 5 alone saves the mixed village; (2) land alone does nothing; (3) a higher bread buyer ceiling lets bakeries bid labour back; (4) debt money indifferent.

Mixed greed (hoarding 0.5), SuMSy, 10 seeds — survived to round 100 / mean alive / mean end round:
capacity 4: 1 / 6.4 / 62 · capacity 5: 0 / 0 / 64 · capacity 6: 1 / 7.3 / 80 · land 2: 0 / 0 / 59 · land 2 + cap 5: 1 / 6.4 / 62 · **bread ceiling 2.0: 8 / 51 / 90** · ceiling 2.0 + cap 5: 7 / 45 / 91 · no greed, cap 5: 10 / 64 / 100.

(1) **wrong** — capacity buys ~15–20 rounds, never survival. (2) held. (3) held on the survival count, **but the survivors are on the same path**: at round 100 the three surviving ceiling-2.0 villages have all labour sold, ~110 units at the theatres, bread down to 117 (below the 128 eaten), price 7.9, hunger 10 — the collapse trajectory forty rounds late. (4) not run (debt arm dropped for time).

Mechanism, as the data show it: the theatre earns ~20 per unit of labour (ten customers at ~2), a bakery under 7 (two loaves at ~3.4 less grain); with `wage_ceiling_from_own_ask` the theatre always wins the wage auction, and bread cannot pull labour back because the buyer's ceiling (`bread_bid_base_multiplier = 1.3` × trailing price) stops the bread price from signalling scarcity. Hunger appears while bread is still affordable — the report's "price of bread outruns the fixed income" is not what happens; output falls because the workers are at the theatre. Capacity and land do not change the revenue-per-worker ratio, so they do not change the outcome; the ceiling narrows it a little and delays it.

**Sentence for Section 10:** under SuMSy, universal mixed greed sets off a labour spiral in which entertainment outbids food for workers; it is a property of the theatre's productivity and the damped bread price, not of the fixed income, and no amount of capacity prevents it.

Open: the ratio itself is a modelling choice (one theatre worker serves ten customers; ticket demand has no ceiling like bread's). A run with a less productive theatre, or a ticket ceiling, is the direct test. The wage-ceiling measure (4 loaves) is the existing lever on this ratio and "saves a third" — consistent.

## Still running / not done
- `threshold` SuMSy (partly in `all64_threshold_rounds.csv`), rung 0 at thresholds, `threshold` debt — resume with `/home/claude/run_cap3.sh`.
- Report Section 10 not edited.

## Threshold results (20 September, 10 seeds)

**Rung 0** (bare bones), alive of 64 / hungry person-rounds / bread price / wealth Gini, SuMSy:
- one-miss rule (threshold 0): 53.1 (min 40) / 254 / 20.7 / 0.88 — the report's "8 %" is 17 % at ten seeds
- unmet 5 %: **64.0 (all seeds) / 122 / 5.1 / 0.16**
- unmet 10 %: 64.0 / 122 / 4.0 / 0.14
Debt: 15.3 / 15.5 / 15.2 alive — unchanged; its rung-0 problem is income, not the ratchet.

So the rung-0 SuMSy loss is entirely the one-miss price ratchet: with a threshold any real market would have, nobody dies, bread stays near its starting price, and the wealth Gini falls from 0.88 to 0.16. Rewrite the rung-0 card and the "price ratchet" conclusion in Section 7 around this; the ratchet was a trigger artefact, not a SuMSy property. (Rung 1's planning margin also removed it, which the report says — the threshold shows it never needed removing.)

**all64 threshold block, SuMSy, 10 seeds** (survived / mean alive / bread / cash Gini):
- no greed: 5 % → 10 / 64 / 6.8 / 0.44; 10 % → 10 / 64 / 6.5 / 0.47 — indistinguishable from threshold 0 (6.7 / 0.44). The threshold is inert once there is a planning margin.
- greedy rich 25 %: 5 % → 3 / 22 ; 10 % → 2 / 25. At threshold 0 (3 seeds) 0, 4, 22 alive. Bistable, survival ~25 %, threshold irrelevant.
- mixed greed (hoarding 0.5): 5 % → 1 / 6.3 — same as the capacity-4 baseline (1 of 10). Consistent with the labour mechanism: the trigger is not the cause.
- mixed greed + wage ceiling 4: 5 % → **0 of 10**. The report's "saves a third" was one seed of three; needs the threshold-0 run at ten seeds before it is dropped, but expect it to go.

Debt arm of the threshold block: running (`run_cap4.log`), resumable.

## Recommended report edits, in order
1. Rung 0 card + Section 7 ratchet paragraph (above).
2. Section 10 mechanism sentence (labour spiral, not price of bread vs fixed income).
3. Greedy-rich and wage-ceiling cells: survival fractions, not means.
4. Every "dead by round X": ranges.

---

# Addendum, 20 September 2026 — theatre capacity (shows × seats)

## Code
`shows_per_round` (0 = unlimited, the old rule) and `seats_per_show` (0 = one seat per person at founding). A theatre can sell at most shows × seats tickets a round and never plans or is allotted more labour than that takes at `customers_per_labour_unit` (`ticket_capacity`, `theatre_labour_cap`; caps in `plan_targets!`, the +1 rule and `ticket_market!`). Default bit-identical. 347 tests. Block `shows` in `all64.jl` (1–4 shows, unlimited, greedy-rich and no-greed at 1 show), SuMSy at 10 seeds (`results/all64_shows_rounds.csv`; the debt arm queued).

## Predictions (written first) and what happened
1. One show (128 seats, ≤ 52 labour units for both theatres): the mixed village survives. **Wrong — 0 of 10, and faster (rounds 28–58).**
2. Collapse returns above some number of shows; 2 borderline, 3 like unlimited. 2 shows: 4 of 10 survive; 3 shows: 0 of 5 so far. Right about where the boundary is, wrong about what is on the other side of it.
3. No-greed village unaffected at 1 show — pending.

## Two ways to die, and the cap chooses between them
At **1 show** every seed dies the same way and it has nothing to do with labour: theatre labour is pinned at 52, bread output sits at the land ceiling (192 = 96 land × 2), the greedy's loaf demand does not all materialise (coin flips), unsold stock knocks the ask down 6 % a round, the bread price falls to ~2.0 against a grain price of 3–4 (per two loaves) plus wages, the bakery's maximum grain bid (expected bread price − ½ wage) drops below the farm's floor, **the grain market freezes, bread output goes from 192 to 0 in one round, and everyone dies three rounds later** — with 96 grain still being harvested and the bakeries holding 100–1,500 in cash. A deflation death: overproduction planned against greedy demand, no cost floor on the ask ratchet, and the grain price locked to a depressed bread price.

At **3 shows and unlimited** the death is the labour drain already described (theatre labour 84–136, bread output collapsing to 14–48 before the freeze). **2 shows** is the crossover: seeds that die show either signature; 4 of 10 avoid both.

So the answer to "if that can save the village, it is a labour-allocation problem tied to capacity numbers" is: capping the theatre does remove the labour drain, and it exposes a second failure underneath. **Prerequisites for a dead village, as the model now states them:** (a) non-essential production can absorb labour at a price essential production cannot match and nobody responds (unlimited or ≥ 3 shows); or (b) the essential good's price is allowed to fall below its input cost and nobody responds (the −6 % ratchet on unsold stock, no floor, grain bid tied to bread price). Both are "no one responds" failures of the price rules, which is the reviewer's §6.2 in both directions. Neither is a property of the fixed income.

Rationing (2 loaves a head first) saved the mixed village in the report; it removes the greedy loaf demand that drives (b) and the ticket spend that drives (a) is then all that is left — consistent.

## Next
- Finish the `shows` block (3, 4, unlimited, greedy rich, no greed; debt arm).
- Direct tests of (b): an ask floor at unit cost (`seller_reservation` already knows the cost; the ratchet ignores it), and/or a grain bid not tied to the trailing bread price. Prediction: with a cost floor, 1 show survives in every seed.
- Then the combined case: cost floor + 2 shows + unmet 5 % — the "responds to both" village. If that lives, Section 10 becomes: greed kills only a village whose prices respond to neither scarcity nor loss.

---

# Addendum, 20 September 2026 (afternoon) — cost floor and the unsold share

## Code
- `ask_floor = :cost` — after adaptation the posted ask is never below `seller_reservation` at age 0, times (1 + `ask_floor_markup_when_short` = 0.05) while the seller's cash is below its reserve (producer) or savings buffer (person). Default `:none` (old behaviour).
- `unsold_share` — the cut on unsold stock now has the same logic as the rise on unmet demand: with s > 0 the unsold units must be at least s of what was offered (the old rule was an absolute 2 units, which at 192 loaves a round fired at 1 % unsold). Default 0 (old rule). Wages keep the old rule.
- Block `floor` in `all64.jl` (1 show + floor; 1/2 shows/unlimited "responsive" = floor + unsold 5 % + unmet 5 %; greedy rich; no greed). 360 tests.
- Note to self: `pkill -f <pattern>` with the pattern in the tool's own command line kills the tool shell — the 15 Sept handoff warned about this and I did it anyway.

## Predictions (written first) and the first cell
1. 1 show + cost floor: survives every seed. **Wrong — 3 of 10 whole, 2 partial (34–35 alive), 5 dead; collapse moved from rounds 28–58 to 80–100.**
2–4 pending (block running, resumable).

## What kills the village now (1 show, cost floor, seed 1, dies round 80)
Round 20 → 65: money in circulation 7.1k → 13.3k, wages 2.6 → 5.6, grain 4.4 → 8.4, bread only 3.5 → 5.4. Unit cost of a loaf at round 65 is (8.4 + 5.6)/2 = 7.0; the floor posts the ask there, but the buyers' ceiling (1.3 × trailing average) keeps the realised price at 5.4, so bakeries sell below cost, their cash drains (1,260 → 290), and at round 70 they cannot pay for grain: the grain market freezes again (96 harvested, none sold, farms then burn their cash on wages), bread output 152 → 0, everyone hungry, dead by 80.

Three deaths, one structure: **bread cannot price**. (a) Unlimited theatre: it cannot bid labour back. (b) Capped theatre, no floor: it deflates below cost and the grain bid, tied to it, falls under the farm floor. (c) Capped theatre with a floor: the floor moves the ask but the buyer ceiling holds the realised price, and input inflation (wages and grain bid up by greedy money) squeezes the margin to nothing. Every route runs through `bread_bid_base_multiplier` = 1.3 × trailing price, the rule the reviewer flagged (§6.2). The floor and the thresholds are right and realistic and they buy 40–50 rounds; they do not touch the ceiling.

Open puzzle worth a look: under SuMSy the money stock nearly doubles in the greedy runs (7k → 13.7k) — GI creation outrunning demurrage while the greedy hold capital, presumably; that inflation is what drives (c).

## Next, in order
1. Finish `floor` (running) and read cells 2–4 against the predictions.
2. The direct test: raise `bread_bid_base_multiplier` (2.0 already run alone: delays ~40 rounds) **together with** the floor, the two thresholds and 2 shows — the "fully responsive" village. Prediction: this is the first configuration in which the mixed-greed village survives in ≥ 8 seeds with hunger near zero.
3. If it does, Section 10's claim becomes a claim about price rules, and the honest SuMSy critique shrinks to the money-stock growth under greed (the open puzzle above).

---

# Addendum, 20 September 2026 (evening) — responsive prices, equilibrium start, clearing

## Code
- `start_at_saturation = :equilibrium`: every person at b + GI/(d + demurrage tax) — the balance where own creation equals own destruction — so the stock starts inside its bounds. Test: stock within ±25 % over 40 rounds, where the norm start rises > 50 %. Pair with `initial_price_multiplier ≈ 1.35` (64-person steady state; the 0.63 in the `saturation` block is a 16-person figure — those runs started with prices half what their money stock implies, which explains "dead within 5 rounds" as much as greed does; rerun them at 1.35 before quoting them).
- The money stock under SuMSy is N·GI − d·(balances above buffer) − tax and nothing else (Stef, 20 Sept). The 7k → 14–15k rise in every norm-start run is convergence, not inflation; the "input inflation" that squeezed the bakeries in the floor-only runs is the price level tracking that transient.
- Blocks `responsive` (ceiling 2.0 + floor + both 5 % thresholds, at 1/2/unlimited shows; equilibrium-start variants) and `clearing` (clearing off: no greed, mixed, greedy rich, responsive). 365 tests. Clearing off does not stall at 64 (25 s / 30 rounds).

## Result 1 — a fully responsive market survives universal mixed greed from the norm start, at any theatre capacity
Mixed greed (hoarding 0.5), SuMSy, 10 seeds, survived / alive / last-20-round bread, hunger, price, theatre labour:
- 1 show: **10 / 64.0** / 175, 0.0, 6.2, 52
- 2 shows: **10 / 63.6** / 154, 0.1, 7.0, 95
- unlimited: **10 / 63.2** / 149, 0.3, 7.2, 100
Healthy, not late: output flat from round 60 to 100 (155 → 148 unlimited; 154 → 154 two shows), hunger ≈ 0, theatre labour steady at ~100 of 256. Against 1/10, 4/10, 1/10 without the price rules. The theatre cap is unnecessary once bread can bid labour back. Prediction (≥ 8/10 at 2 shows) held.

## Result 2 — from the equilibrium start the same village dies, slowly, by hyperinflation
Same rules, `start_at_saturation = :equilibrium`, multiplier 1.35, 10 seeds each: 1 show + floor + thresholds **0 / 5.1 alive**; 2 shows fully responsive **0 / 4.6**; unlimited, no price rules **0 / 0**. No-greed equilibrium start: fine (64, bread 6.2, 2 seeds). Trace (2 shows, responsive, seed 1): every villager starts with 197, so the greedy have ~167 of spendable surplus each from round 1 — 193 tickets and 30–37 greedy loaves a round immediately; unmet demand every round, the 6 % rise compounds under the 2.0 ceiling: bread 6.8 → 11 (r10) → 23 (r20) → 62 (r40) → 89 (r60); wages follow 1:1 with bread (the reservation wage is a meal ÷ capacity), labour sold falls 256 → 154 (r20) → 89 (r40), output falls, cash drains from persons (12.7k → 4.2k) into producers; GI of 5 against bread at 35 by round 30. Deaths from round ~25, 11 alive at 60. **This is the "bread price outruns the fixed income" death the report describes — real here, absent from the norm start.**

Why the two starts differ: the norm start keeps most of the stock in enterprise reserves (demurraged) and persons near their buffer (persons' cash 5.6k of 15k); the equilibrium start hands every person 167 above the buffer (12.7k of 14.7k). Universal, insatiable greed (10 loaves a head; reviewer §6.9) then produces unmet demand in every round, and responsive prices convert permanent excess demand into a permanent 6 %-a-round rise. Responsive prices cure the deflation and labour-drain deaths and create the inflation one; a fixed nominal income is what makes the inflation lethal.

**So the sentence "greed kills only a village whose prices respond to neither scarcity nor loss" (this morning) is wrong as stated.** It holds from the norm start and fails from the equilibrium start. The honest statement: with the money stock at its SuMSy equilibrium, universal mixed greed kills the village whether prices are damped (labour drain / deflation) or responsive (inflation against a fixed GI); greed is survivable only when the greedy are few or the stock is still filling. Which of these is the fair comparison is a judgement call — the equilibrium start is the steady state, but "everyone greedy with 167 to spend" is also the strongest assumption in the model.

## Next, in order
1. `clearing` block (queued; first seed: mixed greed with clearing off dead by round 30 — worse, as predicted: clearing lets a bakery buy grain against the bread it will sell; without it the cash squeeze comes first).
2. Equilibrium start with the greedy rich 25 % (the plausible case) and the hoarding sweep — is the inflation death universal-greed-specific?
3. A GI indexed to the bread price at the equilibrium start (the `indexed` block exists — norm start showed a spiral; at equilibrium it is the direct test of "fixed income is what makes it lethal").
4. Rerun `saturation` at multiplier 1.35.

---

# Addendum, 20 September 2026 (late) — government reserve, fiscal policy, clearing

## Code (395 tests)
- **Government reserve** (`government_reserve_in_rounds`, `government_expense_window`, `surplus_redistribution_share`, `surplus_tax_reduction_share`): target = n rounds of expected outlays (trailing mean of public wages, fees, imputed tax on its own wage bill); the surplus above it is paid out per capita and/or returned by lowering taxes. Before this the government hoarded: in the no-greed SuMSy village its cash reached 3,208 — a fifth of the stock — paying demurrage there (it is exempt from the demurrage *tax* only). `manage_government_reserve!` runs after demurrage. Payout rounds down and is capped at cash (a rounding error killed the first run).
- **Fiscal policy** (`tax_policy` :none / :scale / :brackets, `tax_response_coverage`, `tax_response_step`, `bracket_fixed_rise`, `tax_scale_maximum`, `bracket_rate_maximum`): raises on a shortfall (spending above revenue + reserve gap ÷ reserve rounds), sized to cover the coverage share, cuts on a surplus, revenue never moving more than the step per round. `:scale` = one multiplier on every tax (flat wage, capital, demurrage tax, the whole progressive schedule); `:brackets` = each progressive bracket × (1 + r) + fixed points (the 50 % bracket moves five times the 10 % one), capital and demurrage tax follow the scale, the schedule is not scaled twice. Live bracket rates live in `model.bracket_rates`; the statutory ones stay in the parameters.
- **Starting prices**: one `initial_prices` vector and one multiplier per variant for both systems — this was already so in every script; `all64.jl` now asserts it.
- Blocks: `reserve` (SuMSy), `fiscal` (debt only, 9 variants, queued after the current runs — `run_fiscal.log`), `clearing`. Docs: parameter rows and change log in `docs/dashboard_spec.md`.

## Results

**Reserve rule, no greed, SuMSy, 10 seeds** — works: government cash 280–350 against a target of ~315 (was 3,208). Tax-cut disposal finds the balancing rate by itself: `tax_scale` 0.13, revenue 96 against outlays 99 — the demurrage tax was over-collecting 7:1. Not as predicted: bread +19 % (6.8 → 8.1), persons' cash 5.5k → 6.9–7.3k, tickets 38 → 43–48. The money stock is identical (15.4k); what was destroyed by demurrage on the public balance is now spent.

**Reserve rule with mixed greed, responsive prices** — surplus redistributed: **4 of 10** (was 10 of 10 without the rule); surplus lowers taxes: 3 of 3 so far. The public hoard was sterilising ~110 a round of the greedy's surplus, and paying it out per capita hands it back to the greedy; cutting the tax instead leaves it where it was earned. So the norm-start survival result depends on where the surplus goes — a real finding about fiscal design under SuMSy, and a caveat on yesterday's "responsive prices save it".

**Clearing off, 10 seeds** — the hypothesis that clearing lets greed kill the village is rejected: mixed greed dies by round 17 (was 34–94), greedy rich 0 of 10 (was 2–3); a bakery can no longer buy grain against the bread it sells the same round. The no-greed village *without* clearing is a different economy (bread 3.4 vs 6.8, tickets 128 vs 38, Gini 0.21 vs 0.44) — unexplained, worth its own look since the report's baseline has clearing on.

## Pending
- `reserve`: tax-cut variant, equilibrium-start cells, greedy-rich quarter (the plausible case). `fiscal` sweep. Then the 30-seed reruns of the two bistable report cells.
- Report Sections 7 and 10 still unedited; every sentence quoted from them in these addenda is still in the report as printed.

---

# Addendum, 20 September 2026 (night) — why the no-greed village changed with clearing off: a bug

`share_trades` was **0 in every round of every run with `clearing = false`**. Cause: `operating_net` (receipts − outlays a round) was set only in step 3 of `clear!`; on the immediate-settlement path in `promise!` it was never touched, so `net_history` stayed at zero, `forward_value_per_unit` returned 0, every buyer's valuation was 0, no bid ever cleared a founder's reservation, and the share market was dead. "Clearing off" was silently also "share market off". The differences in the no-greed village (tickets 128 vs 38, ticket price 1.7 vs 15, persons' cash 9.6k vs 5.5k, cash Gini 0.21 vs 0.44) are the rung-7-versus-rung-8 crowding-out effect, not clearing. The greed cells of the `clearing` block are confounded the same way (greedy "capital" purchases are only made below the buyer's valuation, which was 0): the "dead by round 17" is real but not attributable. Rung 10 of the ladder is unaffected (rung 7 has no share market).

Fix: `promise!` now accumulates `operating_net` payment by payment on the immediate path (−paid for the paying enterprise, +paid for the receiving one). Regression test: share trades occur with clearing off. 399 tests. `results/all64_clearing_rounds.csv` deleted and the block re-queued (`run_clr.log`).

Lesson for the reviewer's list: a switch that changes the settlement path must be checked against every quantity derived from settlement; `operating_net` was one, and nothing tested it.

## Reserve block complete (10 seeds, SuMSy) — survived / alive / rounds / tax scale
- mixed greed, responsive, norm start: surplus as **tax cut 10 / 63.3 / 100 / 0.30**; surplus **per capita 4 / 25.5 / 80 / 1.00**. Where the public surplus goes decides survival: cut the over-collecting demurrage tax and the greedy village lives; hand the surplus back per head and it dies in six seeds of ten.
- greedy rich 25 %, equilibrium start, damped prices: **0 / 5 / 80** with the reserve rule (half and half), **0 / 5.6 / 79** without. The plausible case dies from the equilibrium start with the report's price rules. Not yet run: greedy rich + equilibrium + responsive prices — the cell that decides whether the equilibrium-start death is universal-greed-specific or not.
- mixed greed, responsive, equilibrium start, unlimited theatre, half and half: dead by round 5 — the immediate labour drain (193 tickets in round 2 from 167 of surplus a head), same as the responsive block's "unlimited, equilibrium start" without price rules. The theatre cap matters again from the equilibrium start; from the norm start it did not.

Queue (`run_q.log`, `run_clr.log`): fiscal sweep (debt, running), responsive / floor / shows remainders, clearing rerun after the operating-net fix.

---

# Addendum, 20 September 2026 — fiscal sweep (debt village, 10 seeds)

**Bug found first**: the fiscal policy's multiplier reached the capital and demurrage taxes but not the flat wage tax withheld at source (`pay_income!` had its own `gross × wage_tax_rate` line) nor `effective_wage_tax_rate`, so the first sweep tripled the scale for a 20 % revenue gain. Fixed, tested (tax withheld doubles when the scale doubles; the effective rate follows), results deleted and rerun. 401 tests. A test that asserted "the deficit shrinks with the policy on" was replaced by "revenue rises": whether the deficit shrinks is a result, and it turns out to be false.

**Results** (alive of 64 at the end / end round / government debt / tax scale or top bracket / revenue / outlays / wage / bread):
- flat 15 %, reserve rule, no policy: **61.4 / 100 / 14,505** / 1.00 / 72 / 154 / 2.11 / 2.95 — the report's debt village, structural deficit of ~80 a round.
- scale, cover 25 %, 50 % or 100 %, step 2 %: **3.8 / 84 / 7,162** / 3.00 / 44 / 30 / 5.92 / 7.53 — identical across coverage: the shortfall never closes, so the scale climbs at the step every round until its ceiling (45 % wage tax) at round ~60, and the village dies around it.
- scale, step 5 %: **0.8 / 60** — same, faster.
- progressive (Belgian schedule: 25/40/45/50 %, 13 % SSC, 7 % municipal), **no policy: 3.9 / 88** — the schedule alone kills the village: revenue 250–335 a round in rounds 2–5 (against 66 flat), credit refusals climb, bread output falls from 146 to 50 by round 10, 31 alive by round 10.
- brackets, any coverage: **0 / 22**; with +0.5 pt fixed: 0 / 18.

**Reading.** The debt village's deficit is structural — public wages plus a 2-loaf fee against a 15 % tax on a wage bill that cannot bear more. Any rule that raises taxes to close it raises them until the ceiling, and the village dies before the deficit does; with the Belgian schedule it dies without any policy at all. This is `analysis/price_vector.py`'s finding (no feasible price vector at a 30 % wage tax) reproduced dynamically: the wage-tax capacity of this village is somewhere between 15 % and ~25 %, and its public spending needs about twice its revenue. The incremental design works as specified — no step exceeds 2 % — and it is precisely the gradualness that makes the death slow rather than avoided. The downward half of the policy (cuts on surplus) never fires in the debt village because there is never a surplus. The policy is therefore only testable in both directions where a surplus exists: the SuMSy village with the demurrage tax (the `reserve` block showed the cut side working, scale 0.13).

**What this says about the ladder.** Every debt-village rung from 2 on runs a 15 % flat tax and a deficit of 24–45 % of GDP, and now we know why the tax was 15 %: it is roughly the most the village survives. "Keeps everyone alive but runs deficits of 45 % of GDP" (handoff §6) should read "keeps everyone alive *because* it runs deficits of 45 % of GDP: the taxes that would close them kill it". That is a stronger statement for the report than the one it makes.

Not done: a coverage sweep that would find the survivable rate (scale ceiling at 1.3, 1.5, 1.7 instead of 3.0 — `tax_scale_maximum`); the SuMSy side of `:brackets` (no progressive wage tax there, so it is moot).

## Clearing rerun after the operating-net fix (SuMSy, 10 seeds)
The fix works — share trades occur with clearing off (12, 5, 40, 28, 7 a round in rounds 1–5) — but the market then dies by round 6 and stays dead (0–8 a round), where with clearing on it trades 60–80 a round for twenty rounds and 20–30 a round to round 30. The reason is now economic: immediate settlement is cash-in-advance. Firms must hold the money before they buy grain or hire; credit refusals reach 1,157 by round 10 (583 with clearing), enterprise cash sits at ~550 instead of rising to 900–1,300, dividends are 0 in rounds 5–10 (0.5–14 with clearing), forward valuations stay low, and few buyers value a share above the founders' own valuation. Persons keep their cash (9.4k vs 5.5k at round 100) and spend it on tickets (125 vs 38), and the cash Gini is 0.18 vs 0.44.

So: **the rung-8 result — the share market crowds out the theatre and raises the cash Gini to ~0.5 — depends on the clearing system.** With cash-in-advance the same village has a thin share market, three times the tickets and a Gini of 0.18. Clearing was introduced to keep the debt village's bakeries open (payment-order artefact); under SuMSy it also finances the enterprise reserves that fund the dividends that drive the share market. That is worth a paragraph in Section 8 and a line in the reviewer's list: a settlement rule is doing distributional work.

Greed with clearing off (post-fix): mixed greed dead by round 18 (0 of 10); greedy rich 2.9 alive (0 of 10); mixed greed, 2 shows, fully responsive: **61.9 alive, 10 of 10** — the responsive village survives without clearing too.

---

# Addendum, 20 September 2026 — all queued results in (ceiling sweep, floor, shows, responsive complete)

## Ceiling sweep (debt village, flat 15 % × ceiling, `:scale`, cover 50 %, step 2 %, 10 seeds)
| wage-tax ceiling | alive (min) | debt | revenue / outlay | wage | bread | tickets |
|---|---|---|---|---|---|---|
| 15 % (no policy) | 61.4 | 14,505 | 72 / 154 | 2.11 | 2.95 | 34 |
| 18 % | 62.0 (61) | 13,720 | 85 / 162 | 2.10 | 2.95 | 34 |
| 20 % | 61.5 (60) | 13,356 | 97 / 166 | 2.17 | 3.02 | 31 |
| 22.5 % | 61.6 (60) | 12,746 | 109 / 180 | 2.29 | 3.18 | 28 |
| 25 % | 61.1 (59) | 12,435 | 137 / 195 | 2.59 | 3.53 | 29 |
| 30 % | 60.5 (59) | 11,353 | 193 / 241 | 3.23 | 4.21 | 19 |
| 35 % | **33.6 (0)** | 10,383 | 175 / 278 | 5.28 | 6.02 | 7 |
| 45 % (ceiling 3) | 3.8 | 7,162 | 44 / 30 | 5.92 | 7.53 | — |

The village bears a 30 % wage tax and breaks between 30 and 35 % (bimodal at 35: some seeds whole, some dead). But the deficit hardly closes: outlays rise with the tax because public spending is indexed — net public wages are fixed in loaves and grossed up by the effective tax rate, the fee is two loaves — and every rise in the tax pushes wages and bread up (2.1 → 3.2, 2.95 → 4.2 at 30 %). Revenue chases outlays; the gap goes from 82 to 48 a round and the debt from 14.5k to 11.4k, at the cost of half the tickets. The deficit is structural in real terms: public employment plus a fee in loaves cost more than a wage tax can raise. For Section 7: the debt village survives *because* it borrows; the tax that would balance it is above what its economy bears, and the tax it can bear closes 40 % of the gap while eroding consumption.

## SuMSy blocks complete (10 seeds; survived / alive / bread / tickets / cash Gini)
- **floor**: greedy rich 25 %, 2 shows, floor + thresholds: **10 / 64 / 2.1 / 109 / 0.35**. Mixed greed: 1 show + floor 3 / 26; 1 show floor + thresholds 3 / 35; 2 shows floor + thresholds 9 / 62; unlimited + floor 5 / 31; unlimited floor + thresholds 4 / 25. No greed floor + thresholds 10 / 64 / 5.9 / 54 / 0.60.
- **shows** (report prices): mixed greed 1 show 0 / 0 (dead ~49); 2 shows 4 / 30; 3, 4, unlimited identical 1 / 6.4 (theatre labour never reaches the 3-show cap, so the runs are bit-identical). Greedy rich, 1 show: 2 / 34.5. No greed, 1 show: 10 / 64 (the cap does not bind without greed).
- **responsive** (ceiling 2.0 + floor + thresholds): mixed greed 10 / 10 at 1, 2 and unlimited shows (63–64 alive, bread 6.6–7.8, tickets 41–43, Gini 0.47–0.56); equilibrium start 0 / 5 (bread 82–85, the inflation death); no greed 10 / 64 / 5.0 / 32 / **0.71**; no greed equilibrium start 10 / 64 / 5.5 / 105 / 0.31.

Two things to carry: (1) responsive prices raise the cash Gini of the no-greed village from 0.44 to 0.71 — the survival they buy under greed has a distributional price without it, and the report cannot adopt them as "the realistic rules" without reporting that; (2) the greedy quarter from the norm start is comfortably survivable with floor + thresholds and a 2-show cap (10 of 10, bread 2.1, tickets 109), while from the equilibrium start it dies with damped prices (reserve block) — the equilibrium-start greedy quarter *with* responsive prices is the one cell still missing.

All 15 blocks now run under per-subsystem streams at 10 seeds where they were rerun this weekend (capacity, threshold, shows, floor, responsive, clearing, reserve, fiscal, ceiling, contribution) and 3 seeds where not (greed, measures, indexed, share_market, coops, saturation, prices, the ladder). 401 tests.

---

# Staged, 20 September 2026 — consumption tax (implemented, sweep launched)

`consumption_tax_rate` (VAT on bread and tickets, buyer pays price × (1 + rate), tax promised to the government and booked as revenue; buyers' willingness to pay is gross, so the negotiation ceiling is ÷ (1 + rate); grain untaxed), scaled by the fiscal policy with the same triggers and step; `consumption_tax_share` = target share of revenue from consumption (NaN = one scale for all; with a share, separate scales moved within the step so the mix drifts to target while total revenue follows the policy, rebalancing even when total revenue is not asked to move). 418 tests. Block `vat` (10 seeds): debt village — 6 % and 12 % without policy, 6 % with the policy at ceiling 2.0 and mix targets 25 / 50 / 75 / 100 % from consumption; SuMSy — mixed greed, greedy rich and no greed at 6 % and 20 %, with and without responsive prices. Running: `run_vat.log`, resumable.

**Predictions, written before the results.**
1. Debt village, 6 % VAT, no policy: ~25–30 a round of revenue on ~400 of consumption; the deficit falls from ~80 to ~55; survival unchanged; tickets down a little.
2. With the policy and the mix shifted toward consumption: the village bears more total revenue than under the wage tax alone, because a consumption tax does not gross up public wages the way the wage tax does — but the fee in loaves still inflates with the bread price. Expect the deficit to close further at 75–100 % from consumption than at one scale, at the cost of tickets, and the unemployed (whose fee is in loaves, so indexed) to be shielded; the borderline case is whether any mix closes the gap without deaths.
3. SuMSy, mixed greed, damped prices: a 6 % VAT changes nothing (1 of 10 either way); 20 % may delay the labour spiral a few rounds by taxing tickets, no more. With responsive prices: 10 of 10 unchanged at 6 %; at 20 % probably still 10 of 10 with lower tickets.
4. Greedy rich under a 20 % VAT: the greedy buy fewer tickets, so survival should improve from 2–3 of 10; if it does, a consumption tax is a mild brake on greed where a wage tax is not.

## VAT sweep results (10 seeds)

**Debt village** (baseline: 61.4 alive, debt 14,505, deficit 82 a round, tickets 34):
| variant | alive (min) | debt | wage tax | VAT | deficit | tickets |
|---|---|---|---|---|---|---|
| VAT 6 %, no policy | 59.1 (54) | 11,336 | 15 % | 6 % | 37 | 20 |
| VAT 12 %, no policy | 49.1 (41) | 8,840 | 15 % | 12 % | 4 | 11 |
| policy, one scale | 26.5 (15) | 5,918 | 30 % | 12 % | −15 | 2.5 |
| policy, 25 % from consumption | 50.0 | 9,058 | 30 % | 12 % | −30 | 8 |
| policy, 50 % | 57.0 | 10,707 | 8 % | 12 % | 9 | 19 |
| policy, 75–100 % | 59.2 (53) | 11,413 | 2 % | 12 % | 25 | 26 |
Prediction 1 wrong on survival: a 6 % VAT on bread costs ~2 lives a run (the villager who could just afford bread). Prediction 2 held: the consumption tax closes the deficit far more per unit of harm than the wage tax because it does not gross up the public wage bill — outlays fall from 154 to 130 as the mix shifts — and at ≥ 75 % from consumption the policy takes the wage tax to 2 %, the VAT to its ceiling (12 %) and the deficit from 82 to 25, with survival no worse than a flat 6 % VAT. Both taxes rising together (one scale) is the worst case, 26 alive. **The debt village's deficit is closable only by taxing consumption, and taxing consumption at these prices kills.**

**SuMSy** — two findings, one of them about the design of the sweep.
1. With the reserve rule's tax-cut disposal (`surplus_tax_reduction_share = 1`, `tax_policy = :none`) the VAT is scaled away: the SuMSy government over-collects, the cut is applied in full, and `tax_scale` reaches 0 by round 100 in every greed cell (0.08 with no greed). Those cells are therefore uninformative about the VAT (survival identical to the no-VAT references: greedy rich 1–2 of 10, mixed greed damped 0–1, responsive 9–10). Under SuMSy a VAT only exists if the surplus is paid out instead of cut.
2. With the surplus paid out per capita, a 20 % VAT is **lethal even without greed**: no greed 2.4 alive (dead by ~72); greedy rich 0 of 10 at 20 % and at 40 %; mixed greed damped 0; mixed greed responsive 5 of 10 (was 10). Mechanism: buyers' willingness to pay is gross, so the seller sees a ceiling of 1.3 ÷ 1.2 = 1.08 × the trailing price — the tax eats the headroom the bread price needs to follow costs, and the deflation / grain-freeze death follows. At 6 % (1.23×) the no-greed village is fine. So predictions 3–4 fail: a consumption tax is not a brake on greed at any rate that the price system survives, and above ~10 % it harms through the price anchor, not through greed. That is a property of the buyer-ceiling rule as much as of the tax; a sensitivity run with the ceiling raised in step with the VAT is the obvious control and has not been run.

Block `vat` complete (`all64_vat_rounds.csv`). 418 tests.

---

# Addendum, 20 September 2026 (evening) — wealth tax on land and shares at book value

## Code
`wealth_tax_rate` (per year) and `wealth_tax_rounds_per_year` (12). Base per person: land × `land_price` + shareholder units × `book_per_unit` (cash + land − debt per unit) + cooperative membership at capital paid in. Collected every round at rate ÷ 12 in cash after demurrage (`collect_wealth_tax!`); what a person cannot pay is carried as `wealth_tax_arrears` (no interest) and collected first from later cash. Enterprises are not taxed directly (their land and cash are in the book value their owners pay on). **Statutory — the fiscal policy's scale does not move it**: a first version followed the income scale and, under the mix policy (scale ≈ 0.13), was cut to a seventh; those cells were deleted and rerun. 428 tests. Block `wealth` (10 seeds): debt baseline at 1/2/5/10/25 %, best VAT scenario at 2/5/10/25 %, SuMSy greedy rich / mixed greed (damped, responsive) at 2/10/25 %, no greed at 5/25 %.

## Debt village — no, on any scenario
Baseline (61.4 alive, debt 14,505, deficit 82): 1 % → 61.2 / 14,112 / 77; 5 % → 61.2 (min 56) / 12,970 / 70; 10 % → 58.7 / 12,036 / 79; 25 % → 50.7 (min 45) / 10,192 / 70. Revenue tops out at ~27 a round: the base is ~3.8k (I predicted 9–10k) and the tax consumes it (1.3k at 25 %) — book value is what is levied. Wealth Gini 0.70 → 0.43 at 25 %: equalisation by destruction of the base, not by yield. On the best VAT scenario (deficit 25 without it): 2 % → 59.6 alive, deficit 65; 5 % → 57.9 / 71; 10 % → 51.1 / 69; 25 % → 40.0 / 58 — worse at every rate. Part of that is an interaction to fix before quoting: wealth-tax revenue enters `tax_this_round`, the mix policy reads it as income-side revenue against the 75 %-from-consumption target and cuts the wage tax and VAT to compensate. A `wealth_tax_in_mix` switch (exclude it from the mix arithmetic) is the clean fix.

## SuMSy — the first tax that helps against greed
Greedy rich 25 %, damped prices: 2–3 of 10 without → 3 (21 alive) at 2 %, 5 (37) at 10 %, **7 (46) at 25 %**; tickets 44 → 76, cash Gini 0.25–0.30. Mixed greed damped: 0 → 0 / 1 / 3 (19 alive). Mixed greed responsive: 10 / 10 / 10 / 9. No greed: 5 % harmless; 25 % → 62.7 alive, tickets 37 → 23, wealth Gini 0.63 → 0.48. Mechanism, as predicted: the greedy quarter's land and shares are taxed into the government's hoard, which is sterilised (no reserve rule in these cells), their spending falls, the theatre stops draining labour. It works *because* the hoard is not recycled — with the reserve rule paying the surplus out per capita it would return to the greedy (the 20 September reserve result). And the rate that saves the greedy village costs the ordinary one a person and a third of its tickets.

## Standing summary of the weekend's fiscal findings (all 10 seeds, 64 persons, per-subsystem streams)
- Debt village: structural deficit ~80 a round; wage tax survivable to 30 %, closes 40 % of the gap by inflating indexed outlays; VAT closes it far more per unit of harm (no gross-up of public wages) and at ≥ 75 % from consumption takes the deficit to 25 at ~2 deaths a run and half the tickets; wealth tax adds nothing (base too small, consumed by the levy). Both taxes on one scale is the worst case (26 alive). The Belgian progressive schedule alone kills the village.
- SuMSy: the government hoards a fifth of the stock unless a reserve rule is on; the demurrage tax over-collects 7:1; where the surplus goes (tax cut vs per-capita) decides whether the responsive greedy village lives (10 vs 4 of 10); a VAT is scaled away by the tax-cut disposal and, kept, is lethal above ~10 % through the buyer ceiling; a wealth tax on the greedy quarter is the one fiscal instrument that improves survival, monotonically, and only while the proceeds are sterilised.
- Price rules (ceiling, floor, thresholds) decide survival from the norm start and cause hyperinflation from the equilibrium start; the equilibrium-start greedy quarter with responsive prices is still the missing cell.

---

# Addendum, 20 September 2026 (night) — the weighted mix, and what it revealed about the "best scenario"

## Code
`tax_scaling_weights = (income, consumption, wealth)` replaces `consumption_tax_share`. Three scales; when the policy wants revenue to move by r, family i moves by r × w_i ÷ w̄ (w̄ = revenue-weighted mean weight), each step-limited. Equal weights = one scale (the old rule); 0 = statutory; heavier = more of every change. The wealth tax now has its own scale in the mix. 432 tests. Blocks `mix` (11 weight triples, debt), and the `vat` policy cells and `wealth` best-scenario cells rerun under weights.

## Result — weights decide the pace of a raise; they cannot shift revenue between taxes, and that was what the earlier "best scenario" did
Debt village, VAT 6 % + wealth 5 % + wage 15 %, policy at ceiling 2.0, 10 seeds (alive / deficit):
1:1:1 → 14 / 1 · 3:1:1 → 19 / 22 · 1:3:1 → 16 / 3 · 1:1:3 → 12 / 0 · 1:1:0 → 19 / 8 · 1:0:1 → 26 / 20 · 0:3:1 → 42 / 44 · 0:1:1 → 45 / 52 · 0:1:0 → **48 / 44** · 0:0:1 → 54 / 69 · VAT-only policy without wealth tax → 54 / 46.
Every triple with a positive income weight drives all its scales to the ceiling (a chronic shortfall raises every weighted tax by ~2 % a round until it can go no further) and kills two thirds of the village; the deficit closes only when the village is mostly dead. The income-weight-0 triples keep the wage tax at 15 % and land at 42–54 alive with deficits of 44–69.

Compare the share-targeted mix of this afternoon ("75 % from consumption": 59 alive, deficit 25). That rule moved taxes in *opposite* directions — it cut the wage tax to 2 % while raising the VAT to 12 % — and the outlays fell with the wage tax (no gross-up of public wages). Weights cannot do that: a shortfall raises every weighted tax, nothing ever lowers one. **So the best debt-village outcome found this weekend came from shifting the burden from wages to consumption, not from raising anything**, and the weighted policy as specified has no way to reach it. The two mechanisms answer different questions — weights: "which tax bears a raise?"; a share target: "where should revenue come from?" — and the second is the one that found the survivable configuration. Recommendation: keep the weights and put the share target back as a second, optional instrument (a `revenue_mix_target` that rebalances at r = 0, as the share rule did), so the sweep can vary both.

## Missing greed cell
"greedy rich 25 %, 2 shows, fully responsive, equilibrium start" is queued (`run_cell.log`); not in at the time of writing.

---

# Addendum, 20 September 2026 (late night) — the six sliders

## Code
`tax_shift_family` (:none / :income / :consumption / :wealth), `tax_shift_up`, `tax_shift_step` (NaN = `tax_response_step`) replace the weights. The policy's r moves the three family scales (income, consumption, wealth) together; the shift then slides the composition, revenue-neutral, by `tax_shift_step` of revenue a round — the lone family one way, the other two the other way, every move step-limited. Three families × two directions = six sliders. 433 tests. Block `mix` = no shift, the six, plus "shift toward consumption without a wealth tax" and "shift away from income with no policy raise (coverage 0)". `vat` policy cells and `wealth` best-scenario cells rerun under the sliders; the missing greed cell queued after them (`run_sl.log`).

## Result (debt village, VAT 6 % + wealth 5 % + wage 15 %, policy ceiling 2.0, 10 seeds; alive (min) / deficit / income · consumption · wealth scales)
- no shift: 14 (1) / 1 / 2.0 · 2.0 · 2.0 — every scale at the ceiling, the village dead, the budget balanced
- toward income: 27 / 22 / 2.0 · 0.96 · 0.96 · away from consumption: 25 / 13 · away from wealth: 17 / −6
- toward consumption: 45 / 51 / 0.96 · 2.0 · 0.96 · toward wealth: 53 / 71 / 0.96 · 0.96 · 2.0 · away from income: 36 / 43 / 0.96 · 2.0 · 2.0
- toward consumption, no wealth tax: 50 / 47 · **away from income, no policy raise: 55 (52) / 69 / 0.13 · 2.0 · 2.0**

**The slider cannot do what the share rule did, and the reason is the step.** With a chronic shortfall the policy raises every family by the step each round; the shift lowers the "down" families by at most the same step; the two cancel and the down families sit at ~0.96 while the up family goes to the ceiling. The composition never shifts, it only stops one side rising. The wage tax reaches 0.13 only when the policy raise is switched off (coverage 0) — and that cell is the best of the block (55 alive) with the largest deficit (69). This afternoon's share rule reached 59 alive / deficit 25 because it let the down family fall faster than the raise. So as specified the sliders are dominated by the raise whenever there is a shortfall; to work as "one direction / the other direction" they need a shift step larger than the response step (`tax_shift_step` = 0.05 against 0.02, say) — a sweep over that ratio is the next run, not yet made.

Standing lesson of the fiscal week: the debt village's survivable configurations are the ones that *move revenue from wages to consumption*; any rule that only raises kills it, and the only survivable balanced budget found (no shift, deficit 1) has 14 people in it.

---

# Addendum, 20 September 2026 (final) — levers: shift, then move

## Code
`tax_levers = (income, consumption, wealth)` replaces the sliders. Each round the policy first shifts — family i by r × lever_i (positive = with the policy, negative = against, 0 = none) — and then moves every family by r. Total relative move per family = (1 + r·lever)(1 + r) − 1. (0, 0, 0) = one scale; −1 holds a family flat; −2 moves it the opposite way at the policy's rate; the levers turn with the policy, so (−2, +1, 0) relieves wages on a shortfall and cuts consumption first on a surplus. 433 tests. Block `mix` rerun with 14 lever settings; `vat` policy cells, `wealth` best-scenario cells and the missing greed cell queued (`run_lv2.log`, resumable).

## Result (debt village, VAT 6 % + wealth 5 % + wage 15 %, ceiling 2.0, coverage 50 %, 10 seeds; alive (min) / deficit / income · consumption · wealth scale)
- 0:0:0 → 14 (1) / 1 / 2.0 · 2.0 · 2.0 (balanced, dead)
- −1:0:0 → 43 / 46 / 0.96 · 2 · 2 · −2:0:0 → 55 / 71 / 0.12 · 2 · 2 · **−3:0:0 → 58 (56) / 64 / 0.01 · 2 · 2**
- −3:1:0 → 56 / 72 · −2:1:0 → 54 / 75 · −2:2:0 → 52 / 67 · −3:2:1 → 54 / 72 · −2:0:2 → 54 / 74 · −1:−1:2 → 54 / 75
- 1:−1:−1 → 27 / 22 · −1:1:−1 → 45 / 51

The levers do what the sliders could not: a lever of −2 or −3 on income takes the wage tax to 2 % or 0 % while the VAT and wealth tax rise to their ceilings — the shift is no longer cancelled by the raise. Survival then tracks how completely the wage tax is removed (43 → 55 → 58 alive for −1 → −2 → −3), and the deficit sits at 64–75 for every survivable setting: a 12 % VAT plus a 10 % wealth tax on this village raise ~70 a round against outlays of ~135. The two facts of the fiscal week hold under every mechanism tried: **removing the wage tax is what keeps the debt village alive, and nothing that keeps it alive closes more than about half its deficit.** The one balanced budget found (0:0:0) has 14 people in it.

Open: this afternoon's share-rule cell (59 alive, deficit 25, without a wealth tax) is not reproduced by any lever setting with the wealth tax on; the `vat` rerun (levers, no wealth tax) will show whether the wealth tax is what widened the gap — it drains the same book value the founders' dividends come from.

## All runs finished (20 September, 21:45)

**Missing greed cell** — greedy rich 25 %, 2 shows, fully responsive, equilibrium start: **10 of 10, 64 alive, no hunger in any round**, bread 3.8–4.6 (falling from the 6.8 start), tickets ~106, cash Gini 0.31. The equilibrium-start inflation death is specific to universal greed. Full matrix (survived of 10): everyone greedy — damped/norm 1, responsive/norm 10, damped/eq 0, responsive/eq 0; richest quarter — 2–3, 10, 0, **10**. Section 10 sentence: at the steady-state money stock, responsive prices save the plausible greedy village; only universal insatiable greed (the model's strongest assumption) defeats them; with the report's damped prices the price rules kill, not the money system.

**VAT policy cells under levers, no wealth tax** (alive (min) / deficit / income · consumption scale): −3:1:0 → **60.9 (59) / 66** / 0.01 · 2.0; −2:1:0 → 59.1 / 72 / 0.12 · 2.0; VAT 6 % no policy → 59.1 / 59; 1:−1:0 → 39 / 26; 0:0:0 → 26.5 / 7. The lever that abolishes the wage tax gives the best survival of any policy cell — equal to the no-policy village — with the VAT at its 12 % ceiling and a deficit of 66. This afternoon's share-rule cell (59 / 25) is **not reproduced**: no lever setting closes the deficit below ~45 while keeping more than 40 alive. The share rule's revenue split counted the (unscaled) dividend tax on the income side and steered on that; treat its deficit figure as unverified and the lever results as the reference.

**Wealth tax on the best scenario (levers −2:1:0, VAT 6 %)**: 2 % → 58 alive / deficit 61; 5 % → 54 / 75; 10 % → 46 / 67; 25 % → 36 / 46 — worse than without at every rate. The wealth tax drains the book value the founders' dividends (and the dividend tax) come from.

**Standing conclusion of the fiscal week** (10 seeds, per-subsystem streams, 64 persons): the debt village's public deficit is structural (~80 a round) and *not closable by any survivable tax*: the wage tax inflates indexed outlays, the VAT closes the most per unit of harm but a 6 % VAT on bread costs two lives a run, the wealth tax's base is consumed by its own levy, and every mechanism for combining them (weights, sliders, share, levers) finds the same frontier — abolish the wage tax to keep people alive, accept a deficit of 60–70, or balance the budget over a village of fourteen. The SuMSy village has the opposite problem: its demurrage tax over-collects seven to one and the only fiscal instrument that helps against greed (a wealth tax on the greedy quarter) works because the proceeds are sterilised.

---
# 20 September 2026, last — report structure audit and the second ladder
The report's ladder ends on an ownership variant, not on the fiscal endpoint the arc calls for, and every number in it is three seeds on the old stream. `scripts/ladder2.jl` (resumable) rebuilds it: rungs 0–8 as before, 9 co-ops as a side rung with a co-op theatre, then 10 responsive prices, 11 government reserve, 12 fiscal policy + VAT, all at ten seeds; `results/ladder2_64_rounds.csv` / `_summary.csv`. The section-by-section revision plan is `docs/report_revision_plan.md` (also in outputs). Headline: on the cumulative ladder the fiscal endpoint is a trade-off — rung 11 is the best-surviving debt village (63.8 of 64, deficit 95, debt 320 % of GDP), rung 12 the least-indebted survivable one (53.1, deficit 66, 13.5k); the "60.9 alive" block result was on damped prices. Rung 6 debt stops before round 88 — investigate before writing its card. The report HTML itself is unedited.

---
# 21 September 2026 — data loss and repair
`results/ladder2_64_rounds.csv` was truncated by a crash during its rewrite (the script re-read and rewrote the whole 12 MB file after every run; Julia segfaulted at 04:50, EXIT 139). Rungs 4–12 and stress rungs S1–S3 are lost as raw rows; their aggregates survive in `ladder2_64_summary.csv` (22:20, before the rung-6 and rung-12 fixes) and in the conversation tables. Both scripts now append per run instead of rewriting. Rerun launched (`run_l7.log`, resumable): rungs 4–12, S1–S3; S4 is intact. Stress results as read before the loss (10 seeds unless noted): S1 greedy quarter on rung 11 — debt 63.8 alive (theatres closed, tickets 0; the greedy buy shares), SuMSy 64, tickets 113, unemployment 0.8; S2 greedy quarter on rung 12 — debt 55.3 (worst 21), SuMSy 60.1 (worst 25); S3 everyone greedy on rung 11 — debt 64 (6 seeds), SuMSy 64 (9 seeds); S4 everyone greedy on rung 12 — debt 55.8 (5 seeds, worst 45), SuMSy 57.6 (9 of 10 whole, one seed dead), tickets 36, unemployment 2.8, Gini 0.30.
Lever fix (21 Sept): the shift is |r| × lever — a standing preference, not a direction; SuMSy rung 12 now cuts its surcharge fastest on a surplus instead of raising it. Rung 6 redefined (2 % on cash above the working reserve; the 10 % default closed every debt bakery by round 51). Both scripts refuse a SuMSy variant with a wage or capital tax unless named "(comparison)". New report draft: `docs/report_v3_en.md`, Sections 1–13 complete (English). Rerun finished 21 Sept 06:20: every rung and stress rung at 10 seeds, `ladder2_64_rounds.csv` (append-only) and `_summary.csv` current; the rerun reproduced the pre-loss numbers exactly (determinism check).

---
# 21 September 2026 — English HTML report, v3
`scripts/build_report_html.py` builds `docs/html/bread_economy_report_en.html` from `docs/report_v3_en_public.md`: `\[ KADER … \]` frames become `<aside class="frame">`, and eight charts are drawn as inline SVG **from the CSVs at build time** (`ladder2_64_summary.csv` for steps 0–12 and the stress rungs; `all64_capacity/responsive/threshold/floor/ceiling_rounds.csv` for the greed and tax figures), so re-running the script after any rerun updates every graph. Published at https://claude.ai/artifact/2QoeDyc3kT7BSCnso2Jvqt. Vocabulary: guaranteed income, parking fee (demurrage only in the glossary and the parameter frame). Not yet: the other languages, a figure of the price rules, a legend for the Gini.

---
# 21 September 2026 — second critical report received; fixes and reruns
Fixed in `src/` (review 1 §4.1–4.4, §5.1–5.2): bank net wealth no longer adds retained interest to equity that contains it; **enterprise credit affordability is tested on the operating margin** (farm: target × (grain − wage − rent); bakery: target × (2 × bread − grain − wage)), not turnover; the Gini keeps negative values (NaN on a non-positive total) and `negative_wealth_persons` is recorded; the negotiation fallback is clamped into [floor, ceiling]; `fund!` requires cash or credit eligibility when clearing is off; `clear!` records an unbookable net as `clearing_residue` (cumulative, logged) instead of silently zeroing. SuMSy configurations set `dividend_tax_rate = 0` everywhere and the scripts' guard now includes it (review 2 §4.3). Golden values regenerated (both sets); 428 tests.
**Consequence: every result produced before this is superseded** — the credit fix moves every debt-village trajectory (reference village at round 25: bread 10.3 → 6.8, money 3,343 → 2,146) and the SuMSy dividend-tax fix moves rungs 8–12. Previous CSVs moved to `results/pre_review2/`. Rerun queue (`run_rev2.log`, resumable, ~several hours): ladder2 incl. the 2×2 rungs X1/X2 (review 2 §4.4), then share_market, coops, measures, greed, responsive, capacity, threshold at 10 seeds; the remaining blocks after. The report (`report_v3_en_public.md`, HTML) quotes pre-fix numbers until rebuilt.
Still to do from review 2: seed count per section (all blocks to 10); reconcile §9's two datasets (coops block at 10 seeds decides); wage-ceiling sentence from the damped 10-seed cell (measures rerun); disclose the fee / account-fee asymmetries in §2 and rung 2; negative-net-wealth check (column now in every CSV); generate the frames' numbers from the CSVs; commit summaries; reframe §12 around the price rules and the fiscal frontier; derived-quantity tests per switch.

---
# 21 September 2026 — the village grows to 128, four of each kind
Decisions: the ladder's base is now 128 villagers with four farms, four bakeries and four theatres (two at rung 4), founders 4 a firm and target 10 a firm so that every 64-person ratio is kept; the co-op rung changes ownership only (two of each kind) — no 8b/9c controls needed. Theatre seats scale with the village: `seats_per_show = 0` now means villagers ÷ theatres × (1 + `theatre_seat_margin`, default 0.25), and the ladder runs one show a month from rung 4, so the theatres together seat the village plus a quarter. The price rules are behaviour, not policy: rung 10 carries the full realistic set (5 % thresholds, cost floor, buyer ceiling 2.0) and every later rung and stress test inherits it. The best SuMSy village (rung B) = co-op village + rung-10 prices + rung-11 reserve, both systems, with stress rungs SB1/SB2. The government's SuMSy revenue is the *parking tax*, 1 % a month on idle money on top of the 2 % parking fee. `ladder2_128_rounds.csv` (append-only) is the running file; `results/pre_128/` and `results/pre_review2/` hold the superseded 64-person files. After everything is in: one 256-person run with the same twelve firms as the size comparison; then the blocks at 128. 429 tests.

---
# 21 September 2026 — settlement, liquidation and tax families: design closed, implementation next
`docs/design_settlement_and_taxes_2026-09-21.md` is the agreed design (four ways of paying with memberships; invoices payable next round; liquidation at 10 % of book three rounds overdue, refounding by up to four buyers at 90 % of book or the remaining debt, asset liquidation otherwise; struck bank loans written off with the money kept in circulation and the bank recapitalised by the government; peer loans covered from the insurance pool, the rest lost by lenders; five tax families on scales and levers with periods 1 or 12; profit tax with deductibles; parking fee outside). Decisions closed: banks recapitalised (failure a future option), refounded firms keep target and staff, liquidated co-ops refounded as shareholder firms, tax periods default 1 (Belgian withholding mode a future option). Implementation order in §7 of the design; tests listed in §6. The 128 ladder running under the current settlement is the "before" and is to be kept as `results/pre_settlement/` when it finishes. Nothing implemented yet.

---
# 22 September 2026 — settlement ways implemented (design §1)
`settlement = :invoicing` (the design; the default for now is `:clearing_all`, the old system, so goldens still reproduce): `settlement_way(model, from, to, purpose, income_kind)` routes every promise — the government keeps its financing at clearing; wages and rent are end-of-round obligations (`settle_end_of_round!`: paid from cash after the markets, credit for the shortfall, the rest garnishable arrears); anything a person is party to is cash at once; firm→firm is an invoice when both are in `settlement_invoicing ∪ settlement_clearing` (`Invoice` type, payable at the end of the next round, `settle_invoices!` oldest first with credit for the shortfall, unpaid stays open and ages), clearing among `settlement_clearing` members. The wage tax withheld and the enterprise taxes are invoices to the government (government is in the invoicing list). Round order: … greed_spending!, settle_end_of_round!, settle_invoices!, clear!, … Data: invoices_issued/paid/open/overdue. 448 tests. Next: liquidation (design §2), then the tax families (§3). The before-ladder runs from the frozen copy `/home/claude/bread_before` (`run_before.log`).

---
# 22 September 2026 — liquidation implemented (design §2); settlement values renamed
Renamed: `settlement = :invoicing` (the design: cash at the counter, wages and rent at month end, invoices between firms payable next month, clearing only among banks and the government) and `:clearing_all` (the old system, everyone netted together; still the default so earlier results reproduce — to become the comparison once :invoicing is checked and made the default).
Liquidation, all under `:invoicing`: `insolvent` (invoices ≥ 3 rounds overdue ≥ 10 % of book, or book ≤ 0 with anything overdue); `refound!` (sale as a going concern at max(90 % of book, debt left after cash) to up to four buyers with the most spare cash, shares pro rata, overdue invoices paid first, then peer then bank loans; target, staff, stock and land kept; a co-op refounded as a shareholder firm, its locked reserve to the government); otherwise `close_enterprise!` with `write_off_invoices!` (suppliers' bad debt, chains allowed) and `recapitalise_banks!` (the government pays in any negative bank net worth, at its clearing); struck peer loans covered from the bank's insurance pool when `peer_loan_insurance` (or the loan is an insured instalment), the rest the lender's loss. `make_loan!` factored out of `request_loan!` (booking without the credit decision). Data: liquidations, refoundings, bad_debt, bank_bailouts. 468 tests. Next: the tax families (design §3).

---
# 22 September 2026 — ladder at 512 × 120, two fixes
Ladder base: 512 villagers, 120 months (a round is a month), same twelve firms, founders 4 a firm (ownership concentration as in reality), starting targets 10 a firm per 128 villagers, bank rates start 0.5 % a month and are capped at 1.5 % (the rate itself covers the banks' costs, ~1 %/month at both sizes). `bank_customers_per_labour_unit` exists but is off: scaling bank staff with customers quadrupled the banks' costs while their loan book (mostly the twelve firms) did not grow, so the cost-covering rate hit the cap and both banks went under at 512. `SETTLEMENT=clearing_all` runs the same ladder under the old settlement → `ladder2_512_clearing_all_rounds.csv`; the frozen copy is retired. Queue: `queue_512.sh` (invoicing, then clearing_all), resumable, `run_512.log`, ~10–12 h each.
Fixes: (1) EconoSim bug — `process_debt!` returns variables defined only inside `if !debt_settled(debt)`, so it crashes on a settled debt; a refounding pays loans off outside the schedule and left such loans marked open. `service_debt!` now closes a settled loan instead of processing it. **Fixed upstream by Stef the same day** (EconoSim.jl, `finance/debt.jl`: the three results initialised before the `if`); pulled into the local clone, tests pass. The guard in `service_debt!` stays: it also marks the loan settled on our side. (2) Banks could still close: a bank short at settlement borrows from the other bank, and arrears on that loan went to the old seizure. Under :invoicing with `bank_bailout`, a bank in interbank arrears is now recapitalised by the government for what it owes and never seized. 497 tests.

---
# 23 September 2026 — version-stable randomness
Julia 1.13 on Apple Silicon (Stef's machine) failed every golden value: different random paths, not rounding. Julia does not promise the same random sequence, `shuffle`, `rand(collection)`, `rand(1:n)` or `hash` across versions, and Agents.jl keeps agents in a hash map. Fixed so that a seed gives the same run on any version and processor: `src/stable_random.jl` — every stream (and the single-stream mode) is a `StableRNG` (StableRNGs.jl), seeded by FNV-1a of the stream name mixed with the run seed by SplitMix64 (a fixed rule instead of `hash`); `stable_shuffle[!]`, `stable_pick`, `stable_range` use only `rand(rng)`; every dictionary in model state is an `OrderedDict` (insertion order); agents are iterated by id (`agents_by_id`). New dependencies StableRNGs, OrderedCollections. All earlier runs superseded (new draws); golden values regenerated. Four tests that depended on one run's numbers now state their rule. New: a statistical regression test (ten runs of the two reference villages, means within bands) that survives any change that only reshuffles draws. 499 tests. The cross-version check is Stef running the tests on Julia 1.13: the golden values must match to the last digit.

# 23 September 2026 — the Agents.jl warning
`Union{Person, Enterprise}` is not a concrete agent type, so Agents.jl warned at every model creation ("mixed agent models", which its own message says may be silenced). `create_bread_economy` now wraps the model creation in a logger that drops exactly that message (by its source file) and passes everything else through; new dependency Logging. Tidier upstream fix, if wanted: give `create_econo_model` a `warn` keyword and pass it to `StandardABM`. 499 tests, no warnings in the output.

---
# 23 September 2026 — review 3 and the 22 September results (WORK IN PROGRESS: one test failing)
Data: `Review_3.zip` (results of run_all on Stef's machine: 128 invoicing, 512 invoicing, 512 clearing_all). Review: `BreadEconomySim_results_suggestions.md`.
Findings of my own checks against the data and the code:
1. **Public debt under SuMSy was not measured.** `government_debt` counted bank debt, bonds and arrears, not peer loans. A SuMSy government that runs a deficit borrows villagers' savings through peer loans: X1 (SuMSy money, debt fiscal package) borrows ~2,000–2,600 a month (at 128: 194 peer loans, 9,671 owed by month 24) while its column read 0. The reviewer's headline 2×2 rests on this cell. Fixed: `government_debt` now includes peer debt; new column `government_peer_debt`.
2. The same fix showed the SuMSy *reference* test village borrowing 1.5 a month: the government paid the bank's enterprise account fee with no revenue. Fixed: the government pays no account fee. Golden values regenerated.
3. **Still failing:** "paid-in founding" test — a SuMSy configuration whose government now shows peer debt at the end. Not yet traced. Do not quote any SuMSy public-debt figure until it is.
4. **The truncated runs are not crashes:** `check_termination!` ends a run when no farm or no bakery is left ("no farm or no bakery left"). But the data did not record it and the summary's inner join with the last-12-month window silently dropped those runs — survivorship bias, not "honest aggregation" as the review says. Fixed: `termination` column; run_all's summary keeps every run (left join), adds `runs_collapsed`, renames `alive` to `alive_at_end`.
5. **The invoicing cascade** (128, rung 10, debt, seed 1): the four theatres go first, in month 4, by *loan arrears* (book value ≈ −480, no buyer at the debt price → closed); the farms follow in month 8 by the invoice rule firing on overdue of only 6–14 because their book value is negative (−440): firms start with no equity, fund working capital by loans and rent their land, so book ≤ 0 from the start and "10 % of book" is ≤ 0. The review's reading ("invoices_overdue 0 → the trigger is on loan arrears") is right for the theatres. Design questions for Stef: the base of the trigger (book value vs monthly turnover), whether firms are capitalised at founding, and whether the loan-arrears route keeps the old de-minimis rule.

# 23 September 2026 — review 3 acted on (tests pass again)
- The last failing test was real: **SuMSy governments borrow villagers' savings in the first two years**, while the parking tax is small (128 ladder: peaks ~1,000–1,400 around months 17–21, repaid by month 60 at steps 2 and 8, not yet at 11 and B). "Never public debt" holds in steady state only; the invariant is "never a bank loan", which the test now checks. For Stef to decide how the report states it.
- **Liquidation test redesigned (closest to Belgian insolvency law, agreed with Stef):** `liquidation_test = :cash_flow` (default; `:book` keeps the 22 September rule) — overdue obligations (invoices and bank/peer loan instalments, each ≥ 3 months behind) reach 10 % of the firm's monthly turnover (last three months' sales), *and* a loan to pay them is refused; a firm that can borrow catches up and pays. Loan arrears no longer seize a producer under :invoicing — they count in the test. Invoice financing is the firm's own credit: a loan covered by its receivables is made to the firm directly, before any call on founders.
- **Founding equity** (`founding_equity = true` on the ladder, both settlements): founders pay in the firm's working reserve at the founding (savings above the cushion, then a personal loan — bank under debt money, peer under SuMSy). Cooperatives get `shareholder_count` founding members (villagers not founding another firm, by id) who do the same and become its first members.
- Effect at 128, invoicing, debt village, 2 seeds: steps 2, 8, 9, 10, 11, 12, S1, B, SB1 all run 120 months (before: B, 10, 11 and most of 12 stopped by month 8). What remains is economics: all four theatres are liquidated in the debt village from step 8 (demand for ~30 tickets a month cannot carry four), and at B/SB1 three of four farms fail (one farm left). Refoundings are rare — hardly anyone in the debt village can pay the price.
- `run_all.jl` keys saved runs by `MODEL_VERSION = "2026-09-23"`, so a new run never reuses runs of the earlier model; the summary keeps collapsed runs. 527 tests. **Everything must be rerun** (the equity changes every run): `julia --project=. scripts/run_all.jl`.

# 23 September 2026 — SuMSy startup debt against full money stock
New ladder rungs E2, E8, E11, EB: the SuMSy steps 2, 8, 11 and B started at the equilibrium money stock (`start_at_saturation = :equilibrium`, prices × 1.35), SuMSy side only. Summary columns: public_debt_peak, public_debt_peak_month, runs_debt_repaid, public_debt_repaid_month.
Check at 128, one run each: filling up — step 2 peaks 1,032 in month 17, repaid month 46; step 11 1,374 in month 21, repaid month 65; B 1,379 in month 21, repaid month 76. Full money stock — all three peak at 189 in month 1 and are repaid by month 21. My prediction (no startup debt at all) was wrong: the residual is one month's public spending, borrowed because the parking tax is collected at the end of the month, after wages are paid. So: the filling-up debt (~11 a villager, about two months of guaranteed income, for four to six years) is the cost of starting SuMSy below its money stock; at full stock only a one-month timing gap remains; every SuMSy run so far repays its public debt, the debt villages never do. X1 (SuMSy money with the debt village's deficit package) does not repay — repayment comes from SuMSy's fiscal rule together with its money.

# 23 September 2026 — Julia 1.13
The container now runs Julia 1.13.0 (`/home/claude/julia-1.13.0`, `runjl.sh` points at it), the version on Stef's machine. The Manifest is resolved under 1.13.0 and records EconoSim.jl by the relative path `../EconoSim.jl` (the clone now lives at `/home/claude/bread/EconoSim.jl`, next to the repository, as on Stef's machine; `/home/claude/EconoSim.jl` is a link to it), so the same Manifest works on both machines without edits. Note for resolving under 1.13 when the Manifest was made by an older version: load Pkg *before* activating the project (`julia -e 'using Pkg; Pkg.activate("."); Pkg.resolve()'`), otherwise the old Manifest pins stdlib JLLs that 1.13 cannot load. 527 tests pass on 1.13.0 here, golden values unchanged (version-stable randomness).

# 23 September 2026 — EconoSim from GitHub, batch timer
EconoSim.jl is now a dependency by URL (https://github.com/HapponomyOrg/EconoSim.jl, rev main; Manifest records repo-url, repo-rev and the commit), so anyone can instantiate without a local clone; `UPDATE_ECONOSIM=1` in run_all moves to the latest commit on main, `] dev ../EconoSim.jl` switches back to a local copy. run_all times the whole batch (`START_TIME = now()` at the top; "All done in h min s" with start and finish at the end). The local-path repair in run_all is gone.

---
# 24 September 2026 — results of the 23 September model; two findings that invalidate steps 8+
1. SB2 failures (20 runs): refounding a cooperative cleared a non-existent field `patronage`. Fixed; runs completed here (all 1,380 runs in; summaries in results/*_summary.csv). The summary step crashed on missing last-year figures of collapsed runs — fixed (means skip missing; failures print their reason).
2. **Every firm had the same founders.** model.jl assigned `holders = ps[1:shareholder_count]` to every shareholder firm: at 512 villagers persons 12–15 owned all twelve firms (4 owners, not 48). With founding equity each borrowed 2,266 and all four died by month 6 — the "124 of 128" / "508 of 512" pattern. New parameter `founders` (:shared = old, :distinct = each firm its own founders in id order); the ladder uses :distinct. 532 tests. `MODEL_VERSION = "2026-09-24"`.
3. **Still open:** with distinct founders the founders of the liquidated theatres die (step 8 debt: 496 of 512 at month 36 = the 16 founders of the 4 theatres). People personally liable for the founding loan of a failed firm starve — garnishment should leave them food (half rations never kill), so something takes all their cash. To trace before any rerun.
Valid from the 23 September data (steps 0–7 do not depend on the founders): debt village needs a government (step 1: 107 alive of 512, step 2: 512); SuMSy keeps 510–512 alive from step 0; startup public debt under SuMSy at step 2 peaks ~4,060 in month 17 and is repaid by month 46 in all ten runs, at full money stock (E2) it peaks 757 in month 1 and is repaid by month 22 in all ten. Steps 8+ (incl. B, S, SB, X, E8/E11/EB) must be rerun after point 3.

# 24 September 2026 — why the founders die (traced)
Step 8, debt, 512, seed 1: the 16 dead are the 16 **bakery** founders (months 17–20); theatre founders (loan 153) survive and repay by month 21. A bakery founder borrows 273.6 at founding (`make_loan!`, no affordability test), repayable in 20 monthly instalments of 13.7 at 0.5 %; income is 8–12 a month (wage or benefit) plus a little rent. The instalment plus food exceeds income: the bank takes the land parcel by parcel (6 → 0 by month 14), then takes all income; three months without bread, dead in month 18. Two realistic fixes proposed to Stef (not implemented): (1) founding loans on business terms — an affordability test and a longer term; (2) a protected minimum on debt collection (as the Belgian limits on garnishment): repayment and garnishment never take the money for the month's meals, the rest becomes arrears.

# 24 September 2026 — protected minimum and founding loans on business terms (Stef's decisions)
- `collection_floor_in_breads = 1.0` (default one loaf, Stef): bank-loan repayment, garnishment and peer-loan repayment never take a person's cash below one loaf at the expected price; a payment that would is missed (arrears, handled by the existing arrears rules). Changes debt collection everywhere: golden values regenerated.
- Founding loans (debt village): only what passes the affordability test over `founding_loan_term = 60` months; the founder puts in what they can, the firm starts with less equity and borrows the rest itself. SuMSy founders still borrow through `request_loan!` (peer, affordability-tested). `make_loan!` takes a `term` keyword.
- Check at 512, one run, 48 months: steps 2, 8, 9, 11, 12 all 512 alive in both villages (before: 16 bakery founders dead at step 8). The four debt-village theatres are still liquidated from step 8. Oddity to check in the full data: step 9 (co-ops) and step 8 in the debt village show the identical public debt (88,104) at month 48 although other figures differ.
531 tests. MODEL_VERSION 2026-09-24 (not yet run on Stef's machine).

---
# 24 September 2026 — the 24 September run (model 2026-09-24) and review 5
Review 5 (`BreadEconomySim_critical_report_5.md`, on the 23 September data): refounding bug, summary crash and founder deaths were already fixed on 24 September. New data (repo commit 9f360c2): 1,380 runs, all complete. Debt villages from step 8 now keep all 512 alive (founder deaths gone).
Summary fixed (review 5 §2.1, §2.6): a collapsed village is never "whole"; new column `alive_complete_runs`; wealth Gini reported as missing when any run's value leaves [0, 1] (negative net wealth). Collapses remaining under invoicing, debt side: steps 10, 11, B (2 of 10 each), S1, SB1 (1); none under clearing_all.
Transition selection (512 invoicing, collapsed counted as not whole): best = step 8 (10 of 10 whole, 512, public debt 177,105), worst = step 12 (0 whole, 319.6 alive, worst 309). Same pair under clearing_all.
Land market (review 5 §2.3) confirmed on the new data: under SuMSy the farms own 71–77 % of the land at month 120 and 36–40 of 128 landowners remain; under debt money farms own 0–6 % and all 128 keep their land. SuMSy-only settings: `land_price_rent_multiple = 50` (debt: 15) and `instalment_purchases = true`. Decisions pending with Stef: land-market twin runs, household land share and attribution of ownerless firms' equity beside the Gini, whether the transition switches the land market; and whether to drop the "no farm or no bakery" stopping rule.
SuMSy public debt repaid 10 of 10 at steps 2–11, B, E rungs, S1, S3, SB1; not at step 12 (0 of 10, ~1,100 outstanding), S2 (5), S4 (0, ~3,100), SB2 (8). X1: 123,474, never repaid.

---
# 24 September 2026 — the land market (Stef's decisions)
- `land_pricing = :market` (ladder): one base price (15 months' rent) in both villages; the quote moves 6 % on unserved demand or unsold offers, **never above the most an unserved buyer would pay nor below the least an unsold seller would accept**. Land is worth rent ÷ (levy + the best alternative for the money): debt money — the deposit rate (≈ 400 months' rent for an owner), the bank's rate for a borrowing buyer; SuMSy — idle money loses the parking fee and tax, so without a levy land beats money at any price and nobody sells voluntarily (Gesell).
- Seller credit (instalments) in both villages at a **market rate** between the seller's best alternative (debt: deposit rate; SuMSy: lending through the bank, −1.5 %) and the buyer's (debt: the bank's rate; SuMSy: the peer-loan rate, −1 %), split by `instalment_bargaining = 0.5` — positive under debt money, negative under SuMSy.
- Land levy variant (`land_levy`, SuMSy steps L2, L8, L11, LB): the cost of holding money plus `land_levy_margin = 0.25 %` a month, to the government; land then trades (step 8 at 512: 1,153 units in five years at ~57 months' rent). Note for Stef: at that rate the levy takes ~1.85 × the rent (2,057 a month to the government at step 8) — a large fiscal flow.
- Wealth, book values, the wealth tax and the levy value land at the **last voluntary sale price** (`land_price_traded`); distress sales are fire sales and do not count; the starting price until the first voluntary sale. The quote stays in the data as `land_price`.
- Villages run on without farms or bakeries (`stop_without_producers = false`). New columns: land_households_share, gini_wealth_attributed (ownerless firms' equity spread per villager), land_supply/demand/sold, land_levy. 549 tests. MODEL_VERSION 2026-09-24d.

# 24 September 2026 — the levy under the levers, the land dividend, the public-employment sweep
- The land levy is a sixth tax family (`land`), scaled by the fiscal policy like any tax (`land_levy_scale`; lever key `land`). The levy steps L2 and L8 now carry the reserve rule, so the levy is cut when revenue exceeds need.
- New SuMSy steps LD8 and LD11: the levy stays at its neutralising rate and the surplus is paid out per head (`surplus_redistribution_share = 1`) — a land dividend.
- New steps G8-20…G8-50 and G11-20…G11-50, both villages: public employment (employer of last resort, from labour the market leaves idle) at 20–50 % of the village's labour.
- Check at 512, one run, 60 months: L8 — the reserve rule cuts the levy to zero, land freezes again (54 units sold in five years); LD8 — the levy stays, land trades (1,082 units at ~58 months' rent), 62,439 paid out (~2 a villager a month); G8-40 debt — the government employs 40 % of the labour (819 of 2,048 units), unemployment falls to 35, public debt 96,660 by month 60 (no worse per month than at 10 %).
551 tests. MODEL_VERSION 2026-09-24e. The full job is now ~2,040 runs.

# 24 September 2026 — a missing farm or bakery is started again
`refound_minimum = 1` (Stef's default; 0 = off): when fewer farms, or bakeries, are open than that, the `shareholder_count` villagers with the most spare cash (ties by id) try to start one each month. The most recently closed firm of that kind is reopened with a clean slate (new founders and shares, no stock, debts or history, the starting production target) and the founders pay in its working reserve as at the founding (`pay_in_equity!`, factored out of `pay_in_founding_equity!`: savings above the cushion, then a founding loan on business terms or a peer loan; a founder never goes below the protected minimum after the founding). If they raise nothing, the attempt fails and is retried next month. Column `new_firms` (cumulative). 557 tests; golden values unchanged. MODEL_VERSION 2026-09-24f.
